services:
  - type: web
    name: sse-client
    env: static
    buildCommand: cd Client && npm install && npm run build
    staticPublishPath: Client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
